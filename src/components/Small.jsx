import styled from "styled-components";

export const Small = styled.small`
  &.phrase {
    margin-top: 11rem;
    margin-bottom: 4rem;
    color: var(--blue-light-color);
  }

  &.menu-phrase {
    color: var(--blue-light-color);
  }
`;
